#include <bits/stdc++.h>
using namespace std;
int main(){
    //Arithmetic(+,-,*,/,%,++,--)
    //Assignment(=,+=,-=,*=,/=,%=,&=,|=^=,>>=,<<=)
    //Comparison(==,!=,>,<,>=,<=)
    //Logical(&&,||,!)

    //Precidency of operators(In order)
    /*

    () - Parentheses
    *, /, % - Multiplication, Division, Modulus
    +, - - Addition, Subtraction
    >, <, >=, <= - Comparison
    ==, != - Equality
    && - Logical AND
    || - Logical OR
    = - Assignment
    
    */
}